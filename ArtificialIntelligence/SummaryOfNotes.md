博学之, 审问之, 慎思之, 明辨之, 笃行之;
零、壹、贰、叁、肆、伍、陆、柒、捌、玖、拾;







零.Python数分常用开源库和工具(001PyDACommonlyUsedLibraryAndTools)
    
    🧮 NumPy
        Numerical Python 的缩写，是 Python 的科学计算基础库。
        核心是 多维数组（ndarray）。
        支持矩阵运算、线性代数、统计分析等高效数学操作。
        ✅ 类似于：Python 里的「高性能数学计算引擎」。


    📊 Pandas
        基于 NumPy 的 数据分析与处理库。
        核心结构：Series（列） 和 DataFrame（表格）。
        提供数据清洗、筛选、聚合、统计等功能。
        ✅ 类似于：Python 里的「Excel + SQL」。


    📈 Matplotlib
        Python 最基础的 数据可视化库。
        支持折线图、柱状图、散点图、饼图等。
        可高度自定义样式。
        ✅ 类似于：Python 里的「画图工具」。


    🌈 Seaborn
        基于 Matplotlib 封装的 高级统计绘图库。
        绘图更简单、样式更美观。
        常用于可视化数据分布与相关性。
        ✅ 类似于：Matplotlib 的「美化版 + 智能版」。
        

    🐍 Anaconda
        Python 的科学计算与数据分析发行版（Distribution）。
        它是一个 Python 的“打包套装”，里面已经集成了所有的AI/ML工具和库
        如果计算机上已有python，再次安装Anaconda也不会有影响
        
    Conda 是一个开源的包管理器和环境管理器
        它会自动处理包依赖问题，避免“依赖地狱”。
        Anaconda 内置了 Conda，是基于 Conda 的发行版，因此安装 Anaconda 就自动拥有 Conda。
        除了用Anaconda创建和管理虚拟环境以外，还可以通过conda命令行的方式进行
        命令行的创建和管理虚拟环境
            创建虚拟环境（指定python版本的环境）
                conda create -n 虚拟环境名称 python=版本
            进入虚拟环境
                conda activate 虚拟环境名称
            退出虚拟环境
                conda deactivate
            删除虚拟环境
                conda remove -n 虚拟环境名称 --all
            查看所有虚拟环境列表
                conda env list
                conda info --envs
            安装指定包
                conda install numpy
            安装指定版本的包
                conda install pandas=2.1.0
            更新包到最新版本
                conda update matplotlib
            升级 Conda（更新Conda自身）
                conda update conda
            删除包
                conda remove seaborn
            列出当前环境下的所有包
                conda list
            列出当前环境下的指定包
                conda list pandas
            查看 Conda 信息（显示 Conda 安装信息、Python 版本、环境路径等）
                conda info
            查询包的可用版本
                conda search numpy
            导出当前环境的所有包到 yml 文件
                conda env export > environment.yml
            通过导出的 yml 文件重建环境
                conda env create -f environment.yml

    🧑‍💻 Jupyter Notebook / JupyterLab
        交互式Web端编程环境，支持边写代码边运行边展示结果。
        常用于数据分析、机器学习、实验笔记。
        JupyterLab 是它的 增强版 IDE（支持多标签、文件管理、终端等）。
        ✅ 类似于：可运行的「编程笔记本」, 扩展名为 .ipynb
        常用快捷键：
            | 操作              | 快捷键                             |
            | --------------- | ------------------------------- |
            | 运行当前单元          | `Shift + Enter`                 |
            | 插入单元（下方）        | `B`                             |
            | 插入单元（上方）        | `A`                             |
            | 删除单元            | `DD`                            |
            | 切换代码 / Markdown | `M`（Markdown） / `Y`（Code）       |
            | 保存              | `Cmd + S`（Mac）或 `Ctrl + S`（Win） |
        进入base沙箱
            conda activate base
        启动 Jupyter Notebook 服务器
            jupyter notebook
            jupyter notebook --notebook-dir=指定工作目录
        访问启动后的默认8888地址，选择一个目录工作即可。
        pycharm集成jupyter
            选择创建新的项目 File -> New Project
            选择 previously configured interpreter 添加自己本地的 interpreter
            选择 conda environment 就可以看到在conda中新建过的虚拟环境 my_env
            然后右键新建（New）不要选择python file了，选择 Jupyter Notebook即可
            然后可以在 Managed Jupyter server: auto-start 选择Configure Jupyter Server
            选择 Configured Jupyter Server 将 jupyter notebook 命令启动的路径 http://localhost:8888/tree 放到框中即可
            如果有提示要输入密码那 http://localhost:8888/tree?token=xxx 地址后面的token就是密码.
        






壹.NumPy(002NumPy)
    
    TODO...






零、壹、贰、叁、肆、伍、陆、柒、捌、玖、拾;